package io.zipcoder;

import java.util.HashMap;
import java.util.Map;

public class Problem1 {

    String = "The Farmer went to the store to get 1 dollar's worth of fertilizer";

    public static String iterationMethod(){
        Map<Character, Character> newMap = new HashMap<Character, Character>();
        for(Map.Entry<Character, Character> entry : map.entrySet())
            newMap.put(entry.getKey().replace(), entry.getValue());
        map = newMap;

    }

    public static String recursionMethod(){

    }
}
